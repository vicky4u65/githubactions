name: Variable susbstitution
on: push
inputs:
 source:
  required: "true"

jobs:
  json_substitution:
   runs-on: ubuntu-latest
   steps:
     - uses: ./.github/actions
       with:
         source: ${{ env.source }}
         target: ${{ env.target }}